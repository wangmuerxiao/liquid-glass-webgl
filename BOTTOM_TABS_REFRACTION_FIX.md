# 底部标签栏折射偏移问题

## 问题现象

底部标签栏经过液态玻璃折射后，标签图标会偏离原来的位置，通常表现为图标移动到对应标签的左侧，而文字仍然保持居中。

## 问题原因

底部标签栏会先将图标和文字绘制到一张隐藏的 Canvas 中，再将这张 Canvas 作为选中胶囊的玻璃折射内容。

原实现使用 `offsetLeft` 和 `offsetTop` 计算 DOM 元素位置。浏览器对 flex 布局中的 `SVGElement` 读取 `offsetLeft` 不可靠，图标的偏移值可能返回 `0`，导致图标被绘制到标签左边缘；文字的偏移值正常，因此出现图标与文字严重错位。

## 解决方案

修改 `js/components.js` 中的 `drawTintedRow`：

1. 使用 `getBoundingClientRect()` 获取标签、图标和文字的视觉位置。
2. 根据标签行当前的缩放比例和中心位置，将视觉坐标反解为未变换的布局坐标。
3. 使用还原后的坐标绘制图标和文字，并继续围绕各自标签中心执行按压缩放。
4. 保持原有的玻璃折射、拖拽和按压动画逻辑不变。

这样可以同时兼容普通状态、按压缩放状态和位移状态，避免 CSS transform 被错误地混入缓存内容的位置计算。

## 修改文件

- `js/components.js`
  - 替换底部标签栏缓存内容的坐标计算方式。
- `index.html`
- `diagnostic.html`
  - 更新 `components.js` 的缓存版本号，确保加载修复后的脚本。

## 验证结果

- 验证了 3 标签和 4 标签布局。
- 验证了 `translateX + scale(1.2)` 按压状态。
- 图标中心与对应文字中心保持一致，测试误差小于 `0.00002px`。
- 所有 JavaScript 文件语法检查通过。
